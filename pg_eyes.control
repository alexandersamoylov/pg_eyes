# pg_eyes extension

comment = 'PostgreSQL extension for analyze database activity'
default_version = '1.3.2'

requires = pg_stat_statements

encoding = utf8
superuser = true
relocatable = false
schema = eyes
